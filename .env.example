PORT=3000
ANALYSIS_TIMEOUT_MS=8000

# Default reviewer/evaluation mode. Runs locally without API keys.
LLM_PROVIDER=mock

# Optional live provider mode. Do not commit real keys.
# Use LLM_PROVIDER=anthropic with ANTHROPIC_API_KEY for a local live demo.
ANTHROPIC_API_KEY=
